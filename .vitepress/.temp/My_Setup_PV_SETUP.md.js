import{ssrRenderAttrs as i,ssrRenderStyle as e}from"vue/server-renderer";import{useSSRContext as l}from"vue";import{_ as a}from"./plugin-vue_export-helper.BCo6x5W8.js";const s=JSON.parse('{"title":"站点统计维护说明","description":"","frontmatter":{"head":[["link",{"rel":"canonical","href":"https://www.bbgs.xyz/My_Setup/PV_SETUP"}],["meta",{"name":"algolia-title","content":"站点统计维护说明"}],["meta",{"name":"algolia-description","content":"记录技术、生活与思考"}]]},"headers":[],"relativePath":"My_Setup/PV_SETUP.md","filePath":"My_Setup/PV_SETUP.md","lastUpdated":1775043863000}'),o={name:"My_Setup/PV_SETUP.md"};const t=o.setup;o.setup=(i,e)=>{const a=l();return(a.modules||(a.modules=new Set)).add("My_Setup/PV_SETUP.md"),t?t(i,e):void 0};const d=a(o,[["ssrRender",function(l,a,s,o,t,d,p,r){a(`<div${i(o)}><h1 id="站点统计维护说明" tabindex="-1">站点统计维护说明 <a class="header-anchor" href="#站点统计维护说明" aria-label="Permalink to &quot;站点统计维护说明&quot;">​</a></h1><p>这份文档记录当前工程的 PV 和 UV 实现方式、相关配置、部署要点和后续维护注意事项。</p><p>当前统计方案分为两部分：</p><ol><li>PV，总访问量</li><li>UV，累计独立访客</li></ol><p>这两项统计都不会因为普通的前端重新部署而归零，因为它们都不保存在静态页面构建产物中。</p><h2 id="当前架构" tabindex="-1">当前架构 <a class="header-anchor" href="#当前架构" aria-label="Permalink to &quot;当前架构&quot;">​</a></h2><p>当前博客部署结构如下：</p><ol><li>前端站点：GitHub Pages</li><li>域名和 CDN：Cloudflare</li><li>统计中继 API：Vercel</li><li>PV 存储：GitHub Issue 标题</li><li>UV 存储：Upstash Redis</li></ol><p>其中：</p><ol><li>PV 负责统计总访问次数</li><li>UV 负责统计累计独立访客数</li></ol><h2 id="关键文件" tabindex="-1">关键文件 <a class="header-anchor" href="#关键文件" aria-label="Permalink to &quot;关键文件&quot;">​</a></h2><p>以下文件和统计逻辑直接相关：</p><ol><li><p><a href="themeConfig.mjs">themeConfig.mjs</a> 用途：前端统计配置入口，包含 GitHub 仓库信息和 Vercel 中继 API 地址。</p></li><li><p><a href="api/pv-dispatch.js">api/pv-dispatch.js</a> 用途：Vercel Serverless API。负责： 一是把前端 PV 请求转发为 GitHub <code>repository_dispatch</code> 二是根据 <code>visitor_id</code> 进行 UV 去重和累计</p></li><li><p><code>.vitepress/theme/components/Aside/Widgets/SiteData.vue</code> 用途：侧边栏“站点数据”组件。负责： 一是读取 PV 二是生成或复用浏览器本地 <code>visitor_id</code> 三是调用 Vercel API 获取最新 UV 结果</p></li><li><p><a href=".github/workflows/update-pv.yml">.github/workflows/update-pv.yml</a> 用途：GitHub Actions 工作流。负责把指定 Issue 标题中的 <code>PV: N</code> 更新为 <code>PV: N+1</code>。</p></li><li><p><a href="vercel.json">vercel.json</a> 用途：Vercel 路由配置。这里必须排除 <code>/api</code> 路径，否则统计 API 会被错误重写到首页。</p></li></ol><h2 id="pv-实现原理" tabindex="-1">PV 实现原理 <a class="header-anchor" href="#pv-实现原理" aria-label="Permalink to &quot;PV 实现原理&quot;">​</a></h2><p>PV 的实现方式是“每次访问上报一次，然后服务端累加一次”。</p><p>完整链路如下：</p><ol><li>用户打开博客页面</li><li>前端组件 <code>SiteData.vue</code> 挂载</li><li>组件调用 <code>dispatchProxy</code>，也就是当前配置的 Vercel API</li><li>Vercel API 收到请求后，读取服务端环境变量中的 GitHub PAT</li><li>Vercel API 向 GitHub 发起 <code>repository_dispatch</code></li><li>GitHub Actions 工作流 <a href=".github/workflows/update-pv.yml">.github/workflows/update-pv.yml</a> 被触发</li><li>工作流读取指定 Issue 的标题，比如 <code>PV: 123</code></li><li>工作流把它更新成 <code>PV: 124</code></li><li>前端下次读取 Issue 标题时，就能显示最新 PV</li></ol><h3 id="pv-数据存储位置" tabindex="-1">PV 数据存储位置 <a class="header-anchor" href="#pv-数据存储位置" aria-label="Permalink to &quot;PV 数据存储位置&quot;">​</a></h3><p>PV 不是保存在前端，也不是保存在 Vercel。</p><p>PV 实际保存在：</p><ol><li>GitHub 仓库某个 Issue 的标题中</li></ol><p>当前约定格式为：</p><p><code>PV: 123</code></p><h3 id="pv-为什么不会因部署归零" tabindex="-1">PV 为什么不会因部署归零 <a class="header-anchor" href="#pv-为什么不会因部署归零" aria-label="Permalink to &quot;PV 为什么不会因部署归零&quot;">​</a></h3><p>因为 PV 存在 GitHub Issue 标题里，而不是构建产物里。</p><p>只要你没有手动改掉那个 Issue 标题，以下行为都不会让 PV 归零：</p><ol><li>GitHub Pages 重新部署</li><li>Vercel 重新部署</li><li>Cloudflare 刷新缓存</li><li>前端代码更新</li></ol><h2 id="uv-实现原理" tabindex="-1">UV 实现原理 <a class="header-anchor" href="#uv-实现原理" aria-label="Permalink to &quot;UV 实现原理&quot;">​</a></h2><p>UV 的实现方式是“基于浏览器 visitor_id 去重后累计”。</p><p>完整链路如下：</p><ol><li>用户打开博客页面</li><li>前端组件 <code>SiteData.vue</code> 检查本地 <code>localStorage</code> 中是否已有 <code>visitor_id</code></li><li>如果没有，则生成一个新的 <code>visitor_id</code></li><li>前端把这个 <code>visitor_id</code> 一起发给 Vercel API</li><li>Vercel API 收到请求后，到 Upstash Redis 查询该 <code>visitor_id</code> 是否已经见过</li><li>如果没有见过： 把 <code>visitor_id</code> 加入 Redis 去重集合 把累计 UV 总数加 1</li><li>如果已经见过： 不再增加 UV</li><li>Vercel API 把最新 UV 总数返回给前端</li><li>前端更新侧边栏中的“独立访客”数字</li></ol><h3 id="uv-数据存储位置" tabindex="-1">UV 数据存储位置 <a class="header-anchor" href="#uv-数据存储位置" aria-label="Permalink to &quot;UV 数据存储位置&quot;">​</a></h3><p>UV 不是保存在前端页面里，也不是保存在 GitHub Issue 里。</p><p>UV 实际保存在 Upstash Redis 中，主要包含两类数据：</p><ol><li><p>已见访客集合 默认 key 结构： <code>bbgs:visitors:seen</code></p></li><li><p>累计 UV 总数 默认 key 结构： <code>bbgs:stats:uv:total</code></p></li></ol><h3 id="uv-为什么不会因部署归零" tabindex="-1">UV 为什么不会因部署归零 <a class="header-anchor" href="#uv-为什么不会因部署归零" aria-label="Permalink to &quot;UV 为什么不会因部署归零&quot;">​</a></h3><p>因为 UV 存在 Upstash Redis 中，而不是构建产物里。</p><p>只要你没有删除 Redis 数据、没有更换 key 前缀、也没有重置 Upstash 实例，以下行为都不会让 UV 归零：</p><ol><li>GitHub Pages 重新部署</li><li>Vercel 重新部署</li><li>Cloudflare 配置调整</li><li>前端样式或主题更新</li></ol><h2 id="统计口径说明" tabindex="-1">统计口径说明 <a class="header-anchor" href="#统计口径说明" aria-label="Permalink to &quot;统计口径说明&quot;">​</a></h2><h3 id="pv-口径" tabindex="-1">PV 口径 <a class="header-anchor" href="#pv-口径" aria-label="Permalink to &quot;PV 口径&quot;">​</a></h3><p>当前 PV 是按“页面组件挂载一次就上报一次”来累计的。</p><p>这意味着：</p><ol><li>同一个用户连续刷新多次，PV 会继续增加</li><li>同一个用户隔天回来访问，PV 也会继续增加</li></ol><p>这符合常见的 PV 定义。</p><h3 id="uv-口径" tabindex="-1">UV 口径 <a class="header-anchor" href="#uv-口径" aria-label="Permalink to &quot;UV 口径&quot;">​</a></h3><p>当前 UV 是“累计独立浏览器访客”，不是“累计登录用户数”。</p><p>它的实际含义是：</p><ol><li>同一个浏览器、同一个本地存储、同一个 <code>visitor_id</code>，只会被算一次</li><li>用户换浏览器、换设备、清空本地存储、无痕模式访问，可能会被视为新访客</li></ol><p>这对个人博客来说是合理且常见的 UV 口径。</p><h2 id="前端配置" tabindex="-1">前端配置 <a class="header-anchor" href="#前端配置" aria-label="Permalink to &quot;前端配置&quot;">​</a></h2><p>当前前端统计相关配置位于 <a href="themeConfig.mjs">themeConfig.mjs</a>。</p><p>当前关键配置如下：</p><div class="language-js vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">js</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${e({"--shiki-light":"#6F42C1","--shiki-dark":"#B392F0"})}">github</span><span style="${e({"--shiki-light":"#24292E","--shiki-dark":"#E1E4E8"})}">: {</span></span>\n<span class="line"><span style="${e({"--shiki-light":"#6F42C1","--shiki-dark":"#B392F0"})}">  owner</span><span style="${e({"--shiki-light":"#24292E","--shiki-dark":"#E1E4E8"})}">: </span><span style="${e({"--shiki-light":"#032F62","--shiki-dark":"#9ECBFF"})}">&quot;lwb18077512408-sketch&quot;</span><span style="${e({"--shiki-light":"#24292E","--shiki-dark":"#E1E4E8"})}">,</span></span>\n<span class="line"><span style="${e({"--shiki-light":"#6F42C1","--shiki-dark":"#B392F0"})}">  repo</span><span style="${e({"--shiki-light":"#24292E","--shiki-dark":"#E1E4E8"})}">: </span><span style="${e({"--shiki-light":"#032F62","--shiki-dark":"#9ECBFF"})}">&quot;vitepress_blog&quot;</span><span style="${e({"--shiki-light":"#24292E","--shiki-dark":"#E1E4E8"})}">,</span></span>\n<span class="line"><span style="${e({"--shiki-light":"#6F42C1","--shiki-dark":"#B392F0"})}">  pageViewsIssueId</span><span style="${e({"--shiki-light":"#24292E","--shiki-dark":"#E1E4E8"})}">: </span><span style="${e({"--shiki-light":"#005CC5","--shiki-dark":"#79B8FF"})}">1</span><span style="${e({"--shiki-light":"#24292E","--shiki-dark":"#E1E4E8"})}">,</span></span>\n<span class="line"><span style="${e({"--shiki-light":"#6F42C1","--shiki-dark":"#B392F0"})}">  dispatchProxy</span><span style="${e({"--shiki-light":"#24292E","--shiki-dark":"#E1E4E8"})}">: </span><span style="${e({"--shiki-light":"#032F62","--shiki-dark":"#9ECBFF"})}">&quot;https://vitepress-blog-xi.vercel.app/api/pv-dispatch&quot;</span><span style="${e({"--shiki-light":"#24292E","--shiki-dark":"#E1E4E8"})}">,</span></span>\n<span class="line"><span style="${e({"--shiki-light":"#6F42C1","--shiki-dark":"#B392F0"})}">  clientToken</span><span style="${e({"--shiki-light":"#24292E","--shiki-dark":"#E1E4E8"})}">: </span><span style="${e({"--shiki-light":"#032F62","--shiki-dark":"#9ECBFF"})}">&quot;&quot;</span><span style="${e({"--shiki-light":"#24292E","--shiki-dark":"#E1E4E8"})}">,</span></span>\n<span class="line"><span style="${e({"--shiki-light":"#24292E","--shiki-dark":"#E1E4E8"})}">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br></div></div><p>字段说明：</p><ol><li><p><code>owner</code> GitHub 仓库所有者</p></li><li><p><code>repo</code> GitHub 仓库名</p></li><li><p><code>pageViewsIssueId</code> 保存 PV 标题的 Issue 编号</p></li><li><p><code>dispatchProxy</code> 当前统计中继 API 地址 因为博客前端跑在 GitHub Pages，不在 Vercel 同域，所以这里必须使用完整 URL，而不是相对路径</p></li><li><p><code>clientToken</code> 仅用于本地测试时的直连 GitHub API 方案，生产环境保持空字符串</p></li></ol><h2 id="vercel-环境变量" tabindex="-1">Vercel 环境变量 <a class="header-anchor" href="#vercel-环境变量" aria-label="Permalink to &quot;Vercel 环境变量&quot;">​</a></h2><p>当前 Vercel 项目至少需要以下环境变量：</p><h3 id="pv-必需变量" tabindex="-1">PV 必需变量 <a class="header-anchor" href="#pv-必需变量" aria-label="Permalink to &quot;PV 必需变量&quot;">​</a></h3><ol><li><p><code>GITHUB_PAT</code> GitHub Personal Access Token，用于从服务端触发 <code>repository_dispatch</code></p></li><li><p><code>PV_OWNER</code> 当前值：<code>lwb18077512408-sketch</code></p></li><li><p><code>PV_REPO</code> 当前值：<code>vitepress_blog</code></p></li><li><p><code>PV_ISSUE_NUMBER</code> 当前值：<code>1</code></p></li><li><p><code>ALLOWED_ORIGIN</code> 当前值：<code>https://www.bbgs.xyz</code> 用于限制允许调用该 API 的前端域名</p></li></ol><h3 id="uv-必需变量" tabindex="-1">UV 必需变量 <a class="header-anchor" href="#uv-必需变量" aria-label="Permalink to &quot;UV 必需变量&quot;">​</a></h3><ol><li><p><code>UPSTASH_REDIS_REST_URL</code> Upstash Redis 的 REST URL</p></li><li><p><code>UPSTASH_REDIS_REST_TOKEN</code> Upstash Redis 的 REST Token</p></li></ol><h3 id="uv-可选变量" tabindex="-1">UV 可选变量 <a class="header-anchor" href="#uv-可选变量" aria-label="Permalink to &quot;UV 可选变量&quot;">​</a></h3><ol><li><code>UV_KEY_PREFIX</code> 默认值：<code>bbgs</code> 用于 Redis key 前缀隔离</li></ol><h2 id="upstash-redis-当前使用方式" tabindex="-1">Upstash Redis 当前使用方式 <a class="header-anchor" href="#upstash-redis-当前使用方式" aria-label="Permalink to &quot;Upstash Redis 当前使用方式&quot;">​</a></h2><p>当前 UV 方案通过 Upstash REST API 直接请求 Redis，不依赖额外 SDK。</p><p>这样做的好处是：</p><ol><li>适合 Vercel Serverless 场景</li><li>不需要单独引入 Redis 客户端依赖</li><li>后续迁移到其他支持 fetch 的环境也更容易</li></ol><p>当前服务端主要 Redis 操作如下：</p><ol><li><p><code>SADD</code> 把 <code>visitor_id</code> 写入去重集合</p></li><li><p><code>INCR</code> 当且仅当新访客首次进入时，累计 UV 总数加 1</p></li><li><p><code>GET</code> 当访客已经存在时，直接读取当前累计 UV 总数</p></li></ol><h2 id="当前部署关系" tabindex="-1">当前部署关系 <a class="header-anchor" href="#当前部署关系" aria-label="Permalink to &quot;当前部署关系&quot;">​</a></h2><p>当前部署关系建议保持如下：</p><ol><li>博客站点：GitHub Pages</li><li>CDN / 域名代理：Cloudflare</li><li>中继 API：Vercel</li><li>UV 去重存储：Upstash Redis</li></ol><p>这个结构下：</p><ol><li>静态站继续保持简单</li><li>敏感 token 不暴露到前端</li><li>PV 和 UV 都能持久化</li><li>部署互相独立，维护成本低</li></ol><h2 id="验证方法" tabindex="-1">验证方法 <a class="header-anchor" href="#验证方法" aria-label="Permalink to &quot;验证方法&quot;">​</a></h2><h3 id="验证-pv" tabindex="-1">验证 PV <a class="header-anchor" href="#验证-pv" aria-label="Permalink to &quot;验证 PV&quot;">​</a></h3><ol><li>打开博客首页</li><li>刷新一次</li><li>打开 GitHub Actions，确认 <a href=".github/workflows/update-pv.yml">.github/workflows/update-pv.yml</a> 被触发</li><li>检查对应 Issue 标题是否从 <code>PV: N</code> 更新到 <code>PV: N+1</code></li></ol><h3 id="验证-uv" tabindex="-1">验证 UV <a class="header-anchor" href="#验证-uv" aria-label="Permalink to &quot;验证 UV&quot;">​</a></h3><ol><li><p>用当前浏览器正常访问博客，刷新一次 预期：UV 可能增加 1</p></li><li><p>在同一个浏览器再次刷新 预期：UV 不再增加</p></li><li><p>用无痕窗口或另一个浏览器访问博客 预期：UV 增加 1</p></li></ol><h2 id="常见问题" tabindex="-1">常见问题 <a class="header-anchor" href="#常见问题" aria-label="Permalink to &quot;常见问题&quot;">​</a></h2><h3 id="_1-打开-api-pv-dispatch-显示首页" tabindex="-1">1. 打开 <code>/api/pv-dispatch</code> 显示首页 <a class="header-anchor" href="#_1-打开-api-pv-dispatch-显示首页" aria-label="Permalink to &quot;1. 打开 \`/api/pv-dispatch\` 显示首页&quot;">​</a></h3><p>原因： 通常是 <a href="vercel.json">vercel.json</a> 的 rewrite 把 <code>/api</code> 也改写到首页了。</p><p>当前正确做法是：</p><ol><li>保留首页 rewrite</li><li>明确排除 <code>/api</code> 路径</li></ol><h3 id="_2-pv-增加-但-uv-不变" tabindex="-1">2. PV 增加，但 UV 不变 <a class="header-anchor" href="#_2-pv-增加-但-uv-不变" aria-label="Permalink to &quot;2. PV 增加，但 UV 不变&quot;">​</a></h3><p>优先检查：</p><ol><li>Vercel 是否已配置 <code>UPSTASH_REDIS_REST_URL</code></li><li>Vercel 是否已配置 <code>UPSTASH_REDIS_REST_TOKEN</code></li><li>Vercel 是否重新部署过</li></ol><h3 id="_3-uv-每次刷新都在加" tabindex="-1">3. UV 每次刷新都在加 <a class="header-anchor" href="#_3-uv-每次刷新都在加" aria-label="Permalink to &quot;3. UV 每次刷新都在加&quot;">​</a></h3><p>优先检查：</p><ol><li>浏览器里 <code>localStorage</code> 的 <code>visitor_id</code> 是否每次都被重新生成</li><li>Vercel API 是否正确把 <code>visitor_id</code> 发到了服务端</li><li>Redis <code>SADD</code> 是否成功执行</li></ol><h3 id="_4-部署后统计数值看起来丢了" tabindex="-1">4. 部署后统计数值看起来丢了 <a class="header-anchor" href="#_4-部署后统计数值看起来丢了" aria-label="Permalink to &quot;4. 部署后统计数值看起来丢了&quot;">​</a></h3><p>优先检查：</p><ol><li>PV Issue 标题是否还在</li><li>Upstash Redis 数据是否还在</li><li>Vercel 环境变量是否丢失</li><li><code>UV_KEY_PREFIX</code> 是否被改动</li></ol><h2 id="后续维护建议" tabindex="-1">后续维护建议 <a class="header-anchor" href="#后续维护建议" aria-label="Permalink to &quot;后续维护建议&quot;">​</a></h2><ol><li>不要把 <code>GITHUB_PAT</code> 写进前端配置文件</li><li>不要把 <code>UPSTASH_REDIS_REST_TOKEN</code> 泄露到公开页面或仓库里</li><li>如果更换 Vercel 项目域名，记得同步更新 <a href="themeConfig.mjs">themeConfig.mjs</a> 中的 <code>dispatchProxy</code></li><li>如果更换 GitHub 仓库或 Issue 编号，记得同步更新： <ul><li><a href="themeConfig.mjs">themeConfig.mjs</a></li><li>Vercel 环境变量中的 <code>PV_OWNER</code>、<code>PV_REPO</code>、<code>PV_ISSUE_NUMBER</code></li></ul></li><li>如果重建 Upstash 数据库，旧 UV 不会自动迁移，需要你手动保留或接受重置</li></ol><h2 id="维护结论" tabindex="-1">维护结论 <a class="header-anchor" href="#维护结论" aria-label="Permalink to &quot;维护结论&quot;">​</a></h2><p>当前工程的统计方案可以简单概括为：</p><ol><li>PV 存 GitHub Issue</li><li>UV 存 Upstash Redis</li><li>前端通过 Vercel API 统一上报</li><li>GitHub Pages 只负责展示，不保存统计状态</li></ol><p>因此，日后维护时只要你记住一句话就够了：</p><p>“页面负责触发，Vercel 负责中继，GitHub 负责 PV，Redis 负责 UV。”</p></div>`)}]]);export{s as __pageData,d as default};
